

class StringCalculator {
  // 컨트롤러에 의해 1번이 실행되면 마지막 4번 함수에 의해 반환될 때까지 내부에서만 작업하면 됨

  // 1. 커스텀 구분자로 시작하는지 아닌지 판단하여 커스텀 구분자가 있는 경우 2번, 아닌 경우 3번 호출

  // 2. 커스텀 구분자 분리하여 구분자 추출하는 함수

  // 3. 기본 구분자를 포함한 커스텀 구분자를 통해 숫자 추출하는 함수

  // 4. 추출되어 저장된 각 숫자들을 모두 더하여 반환하는 함수 (큰 수도 고려)
}

export default StringCalculator;