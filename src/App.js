import { Console } from "@woowacourse/mission-utils";

class App {
  async run() {
    
    let userInput;
    while(true){
      userInput = await Console.readLineAsync('');


    }

  }
}

export default App;
