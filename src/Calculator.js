import { MissionUtils } from "@woowacourse/mission-utils";

class Calculator {
    calculate(string) {
      try {
        return string+string;
      } catch (error) {
      }
    }
  }
  
export default Calculator;
  

