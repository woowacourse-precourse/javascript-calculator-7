export default class Controller {
  async control(model, viewIn, viewOut) {
    const input = await viewIn.getInput();

    // Todo: 입력값으로 부터 숫자 추출하기
    // Todo: 결과값 구하기
    // Todo: 사용자에게 결과값 보여주기
  }
}
