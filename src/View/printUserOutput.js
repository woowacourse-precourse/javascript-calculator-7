import { Console } from '@woowacourse/mission-utils';

// TO DO: userOutput -> printUserOutput / 함수명은 동작 + ~ /
const printUserOutput = totalNumberSum => {
  Console.print(`결과 : ${totalNumberSum}`);
};

export default printUserOutput;
