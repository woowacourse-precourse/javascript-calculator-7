import { Console } from "@woowacourse/mission-utils";

class SumOutputView {
  constructor() {}
  printOutput(result) {
    Console.print(`연산결과는 ${result} 입니다.`);
  }
}

export default SumOutputView;
